# Дух Храма 2D — before spell pass (metadata-only)

Каталог сохранён как след попытки `spell-pass`, но отдельный `index.html` для этой итерации не сохранился.

## Что подтверждено
- в момент этой попытки предыдущая broken-итерация уже потеряла `current index.html`
- в архив попали только служебные файлы, без рабочего HTML
- для восстановления published current была использована последняя реально валидная commit-версия до поломки публикации

## Важно
- не использовать этот каталог как source-of-truth для патчей
- для публикации брать `games-pages/duh-hrama-2d/index.html`
